---
import { formatEpisodeDuration, formatHebrewDate } from '@/lib/episodes';

interface Props {
  date: Date;
  duration?: string;
  showDuration?: boolean;
  textClass?: string;
  class?: string;
}

const {
  date,
  duration,
  showDuration = true,
  textClass = 'label-marginalia',
  class: className,
} = Astro.props;
---

<div class:list={['flex items-center gap-3', className]}>
  <span class={textClass}>{formatHebrewDate(date)}</span>
  {showDuration && duration && <span class={textClass}>{formatEpisodeDuration(duration)}</span>}
</div>
